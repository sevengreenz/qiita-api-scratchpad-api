service: qiita-api-client

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs6.10
  environment:
    CLIENT_ID: ${env:QIITA_CLIENT_ID}

functions:
  hello:
    handler: build/handlers/hello.hello
    events:
      - http:
          path: hello
          method: get
  getSchema:
    handler: build/handlers/qiita.qiita
    events:
      - http:
          path: qiita/schema
          method: get
  execApi:
    handler: build/handlers/qiita.call
    events:
      - http:
          path: qiita/api
          method: post
          cors: true
  authorize:
    handler: build/handlers/qiita.authorize
    events:
      - http:
          path: qiita/authorize
          method: get


