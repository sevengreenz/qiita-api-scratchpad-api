service: qiita-api-client

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs6.10
  environment:
    CLIENT_ID: ${env:QIITA_CLIENT_ID}

functions:
  hello:
    handler: build/handlers/hello-handler.hello
    events:
      - http:
          path: hello
          method: get
  getSchema:
    handler: build/handlers/qiita-handler.qiita
    events:
      - http:
          path: qiita/schema
          method: get
  execApi:
    handler: build/handlers/qiita-handler.call
    events:
      - http:
          path: qiita/api
          method: post
          cors: true
  authorize:
    handler: build/handlers/qiita-handler.authorize
    events:
      - http:
          path: qiita/authorize
          method: get


